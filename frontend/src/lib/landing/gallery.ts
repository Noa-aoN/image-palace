/**
 * 最初のページに並べる、実際の画面。
 *
 * **空の枠は出さない。** 「準備中」と書いた枠が4つ並ぶくらいなら、
 * その節ごと出さないほうがよい（初めて来た人が最初に見る面なので、
 * 用意できていないことだけが伝わる）。
 *
 * ここに1件でも足すと、節がそのまま戻る。撮った絵を `public/lp/` に置いて、
 * この並びに書き足すだけでよい。
 */
export type LandingShot = {
  /** `public/` からの絶対パス */
  src: string
  /** 何の画面か（枠の下に出る） */
  label: string
}

export const LANDING_SHOTS: LandingShot[] = []
