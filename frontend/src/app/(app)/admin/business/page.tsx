'use client'

import { AdminBusinessPanel } from '@/components/features/admin/AdminBusinessPanel'

// 経営の数字（成長・売上・原価）。運営ダッシュボードとは見るものが違う
export default function AdminBusinessPage() {
  return <AdminBusinessPanel />
}
