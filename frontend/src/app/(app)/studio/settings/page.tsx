'use client'

import { StudioSettingsPanel } from '@/components/features/studio/StudioSettingsPanel'

// 工房の設定。枠の上限と、体験用の宮殿の入口
export default function StudioSettingsPage() {
  return <StudioSettingsPanel />
}
