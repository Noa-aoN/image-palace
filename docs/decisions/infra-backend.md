# ADR: バックエンドインフラ選定

- **日付**: 2026-03-17
- **ステータス**: 承認済み
- **決定者**: チーム

---

## コンテキスト

MVP フェーズのバックエンドをどこにホストするかを決定する必要がある。
要件：

- Rails 8 API + ActiveJob + Redis が動作すること
- Dockerfile ベースでのデプロイが可能なこと
- 初期コストを最小化（月額 $0〜$25 程度）
- 将来のスケールアップ時に移行コストが低いこと

---

## 検討した選択肢

### A. Render
- **メリット**: free tier あり、GitHub 連携が簡単、PostgreSQL アドオンも利用可
- **デメリット**: free tier はスピンダウンあり（コールドスタート数十秒）

### B. Railway
- **メリット**: Dockerfile そのまま動作、開発者体験が良い、プレビュー環境が容易
- **デメリット**: free tier が Render より少ない

### C. Fly.io
- **メリット**: エッジデプロイ可能、無料枠あり
- **デメリット**: 学習コストが高め

### D. Cloud Run（Google Cloud）
- **メリット**: スケールゼロ〜大規模まで対応、コンテナ直接デプロイ
- **デメリット**: IAM・VPC 等の設定コストが高く MVP には過剰

---

## 決定

**MVP フェーズ: Fly.io を選択**

移行トリガー（以下のいずれかを満たしたら Cloud Run へ移行）:
- 月額コストが $100 を超えた
- 月間リクエスト数が急増しリソース上限に達した
- レイテンシ要件が厳しくなりエッジ・リージョン選択が必要になった

---

## 理由

- Dockerfile さえ整備されていれば移行コストは数時間
- MVP では速度・コスト優先のため、設定がシンプルな Fly.io を使う
- Cloud Run は移行先として明示的に設計に組み込んでおくことで、スケール時の意思決定を迷わない

---

## 結果・影響

- `backend/` に `Dockerfile` を整備する（ローカル・本番共通）
- 環境変数は Fly.io のダッシュボードで管理（`.env` はコミットしない）
- DB は Neon（PostgreSQL サーバーレス）を使用し、バックエンドホストと独立して管理
- Fly.io アプリ名: `image-palace-api`、リージョン: `nrt`（東京）

---

## 追記: ジョブキュー設計変更（2026-03-31）

当初の要件に「ActiveJob + Redis」と記載していたが、**Solid Queue（Rails 8 標準）を採用**したため変更。

- Redis（Upstash）は不要になった
- Solid Queue は PostgreSQL をキューとして使用するため、インフラがシンプルになった
- Fly.io 上で Rails と Solid Queue が同一プロセス（または同一 Dockerfile）で動作
- 関連コミット: `feat: 画像生成をSolid Queueジョブに移行し非同期化する` (#61)
